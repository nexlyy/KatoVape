node_modules/

# демо-бэкенд: локальная БД и зависимости не коммитим
server/*.db
server/*.db-journal
server/*.db-wal
server/node_modules/
server/katovape.db-shm
server/katovape.db-wal

supabase/.temp/

# выгрузки базы: там телефоны и адреса клиентов, в репозитории им не место
backup-*.sql
*.dump

# локальные настройки Claude Code, в репозитории им делать нечего
.claude/

# выгрузки базы в любом виде: там телефоны и адреса клиентов
backup-*.json
backup-*

# примерка вёрстки, собирается скриптом вне репозитория
_row-preview.html
